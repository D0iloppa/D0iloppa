<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dictionarySqlMap">

	<select id="getDictionary" resultType="DictionarySearchVO">
		select * from  p_dictionary
		order by abbr asc
	</select>
	
	<select id="findDictionaryAbbr" parameterType="DictionarySearchVO" resultType="DictionarySearchVO">
		select * from p_dictionary
		where abbr like concat('%',#{searchAbbr},'%')
		and eng_full_nm like concat('%',#{searchEng},'%')
		and kor_full_nm like concat('%',#{searchKor},'%')
		and dic_desc like concat('%',#{searchDesc},'%')
		order by abbr asc
	</select>
	
	<insert id="insertDictionary" parameterType="DictionarySearchVO">
		insert into p_dictionary(abbr,eng_full_nm,kor_full_nm,dic_desc,reg_id)
     	values(#{abbr},#{eng_full_nm},#{kor_full_nm},#{dic_desc},#{reg_id})
	</insert>
	
	<update id="updateDictionary" parameterType="DictionarySearchVO">
		update p_dictionary
		set abbr = #{abbr}, eng_full_nm = #{eng_full_nm}, kor_full_nm = #{kor_full_nm}, dic_desc = #{dic_desc}
		where abbr_id=#{abbr_id}
	</update>
	
	<delete id="deleteDictionary" parameterType="DictionarySearchVO">
		delete from p_dictionary
		where abbr_id=#{abbr_id}
	</delete>
	
	<delete id="deleteDictionaryAll" parameterType="DictionarySearchVO">
		delete from p_dictionary
	</delete>
</mapper>